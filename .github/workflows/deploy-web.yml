name: Deploy Web to Homelab
on:
  push:
    branches: ["main"]
    paths:
      - 'apps/web/**'
      - 'packages/shared/**'
      - 'Dockerfile.web'
      - 'package.json'
      - 'package-lock.json'
      - 'turbo.json'
      - '.github/workflows/deploy-web.yml'


env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ritikkumar27/media-force

jobs:
  build-and-push-web:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{env.REGISTRY}}
          username: ${{github.actor}}
          password: ${{secrets.GITHUB_TOKEN}}
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Build and push Web image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.web
          push: true
          tags: |
            ${{env.REGISTRY}}/${{env.IMAGE_NAME}}-web:latest
            ${{env.REGISTRY}}/${{env.IMAGE_NAME}}-web:${{ github.sha }}
          cache-from: type=gha,scope=web
          cache-to: type=gha,mode=max,scope=web

  deploy:
    needs: build-and-push-web
    runs-on: homelab mediaforce
    steps:
      - name: Pull and Deploy on Homelab
        run: |
          echo "${{secrets.GITHUB_TOKEN}}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
          cd /srv/docker/apps/mediaforce
          docker compose pull
          docker compose up -d
